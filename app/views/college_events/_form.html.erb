<%= simple_form_for(@college_event) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :title %>
    <%= f.input :desc %>
    <%= f.association :department %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
